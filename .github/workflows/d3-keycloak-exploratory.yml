name: JLMIRROR D3-A Keycloak Pinned Evidence

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  workflow_dispatch:

permissions:
  contents: read

jobs:
  keycloak-backchannel:
    name: Keycloak 26.7.2 pinned browser + back-channel contracts
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      KEYCLOAK_IMAGE: quay.io/keycloak/keycloak@sha256:831330513f55695572286e521f94fcd3c7e285250ed5b848090265a33192f669
      KEYCLOAK_EXPECTED_DIGEST: quay.io/keycloak/keycloak@sha256:831330513f55695572286e521f94fcd3c7e285250ed5b848090265a33192f669
      KEYCLOAK_ADMIN_USER: admin
      KEYCLOAK_ADMIN_PASSWORD: d3-admin-password
      KEYCLOAK_BASE_URL: https://127.0.0.1:8443
    steps:
      - name: Checkout exact analyzed HEAD
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
          allow-unsafe-pr-checkout: false

      - name: Verify exact commit identity
        shell: bash
        run: |
          set -euo pipefail
          actual_sha="$(git rev-parse HEAD)"
          printf 'expected_sha=%s\nactual_sha=%s\n' "$EXPECTED_SHA" "$actual_sha"
          test "$actual_sha" = "$EXPECTED_SHA"

      - name: Validate D3 machine-owned state
        shell: bash
        run: python3 tools/assurance/validate_d3_identity_security_state.py .

      - name: Pull immutable Keycloak candidate
        shell: bash
        run: |
          set -euo pipefail
          case "$KEYCLOAK_IMAGE" in
            quay.io/keycloak/keycloak@sha256:*) ;;
            *) echo 'candidate must be digest-pinned' >&2; exit 1 ;;
          esac
          test "$KEYCLOAK_IMAGE" = "$KEYCLOAK_EXPECTED_DIGEST"
          docker pull "$KEYCLOAK_IMAGE"
          image_id="$(docker image inspect "$KEYCLOAK_IMAGE" --format '{{.Id}}')"
          test -n "$image_id"
          printf 'keycloak_pinned_digest=%s\nkeycloak_image_id=%s\n' "$KEYCLOAK_IMAGE" "$image_id"

      - name: Generate ephemeral TLS evidence identity
        shell: bash
        run: |
          set -euo pipefail
          tls_dir="${RUNNER_TEMP}/d3-keycloak-tls"
          server_dir="$tls_dir/server"
          mkdir -p "$server_dir"

          openssl req -x509 -newkey rsa:2048 -nodes \
            -keyout "$tls_dir/ca.key" \
            -out "$tls_dir/ca.crt" \
            -days 1 \
            -subj '/CN=JLMirror D3 ephemeral evidence CA' \
            -addext 'basicConstraints=critical,CA:TRUE' \
            -addext 'keyUsage=critical,keyCertSign,cRLSign'

          openssl req -newkey rsa:2048 -nodes \
            -keyout "$server_dir/server.key" \
            -out "$tls_dir/server.csr" \
            -subj '/CN=localhost' \
            -addext 'subjectAltName=DNS:localhost,IP:127.0.0.1'

          cat > "$tls_dir/server.ext" <<'EOF'
          basicConstraints=critical,CA:FALSE
          keyUsage=critical,digitalSignature,keyEncipherment
          extendedKeyUsage=serverAuth
          subjectAltName=DNS:localhost,IP:127.0.0.1
          EOF

          openssl x509 -req \
            -in "$tls_dir/server.csr" \
            -CA "$tls_dir/ca.crt" \
            -CAkey "$tls_dir/ca.key" \
            -CAcreateserial \
            -out "$server_dir/server.crt" \
            -days 1 \
            -sha256 \
            -extfile "$tls_dir/server.ext"

          openssl verify -CAfile "$tls_dir/ca.crt" "$server_dir/server.crt"
          openssl x509 -in "$server_dir/server.crt" -noout -checkend 0

          image_user="$(docker image inspect "$KEYCLOAK_IMAGE" --format '{{.Config.User}}')"
          case "$image_user" in
            ''|root|0) keycloak_uid=0 ;;
            *:*) keycloak_uid="${image_user%%:*}" ;;
            *) keycloak_uid="$image_user" ;;
          esac
          case "$keycloak_uid" in
            ''|*[!0-9]*)
              echo "Keycloak image declares unsupported non-numeric container user: $image_user" >&2
              exit 1
              ;;
          esac

          chmod 0400 "$tls_dir/ca.key" "$server_dir/server.key"
          chmod 0444 "$tls_dir/ca.crt" "$server_dir/server.crt"
          sudo chown "$keycloak_uid" "$server_dir/server.key"

          test "$(stat -c '%u' "$server_dir/server.key")" = "$keycloak_uid"
          test "$(stat -c '%a' "$server_dir/server.key")" = "400"
          printf 'keycloak_tls_container_uid=%s\n' "$keycloak_uid"
          printf 'SSL_CERT_FILE=%s/ca.crt\n' "$tls_dir" >> "$GITHUB_ENV"

      - name: Start isolated TLS Keycloak candidate
        shell: bash
        run: |
          set -euo pipefail
          docker run -d \
            --name jlmirror-d3-keycloak \
            --add-host=host.docker.internal:host-gateway \
            -p 127.0.0.1:8443:8443 \
            -v "${RUNNER_TEMP}/d3-keycloak-tls/server:/opt/keycloak/conf/d3-tls:ro" \
            -e KC_BOOTSTRAP_ADMIN_USERNAME="$KEYCLOAK_ADMIN_USER" \
            -e KC_BOOTSTRAP_ADMIN_PASSWORD="$KEYCLOAK_ADMIN_PASSWORD" \
            "$KEYCLOAK_IMAGE" \
            start-dev \
            --http-enabled=false \
            --https-certificate-file=/opt/keycloak/conf/d3-tls/server.crt \
            --https-certificate-key-file=/opt/keycloak/conf/d3-tls/server.key

      - name: Wait for TLS Keycloak readiness
        shell: bash
        run: |
          set -euo pipefail
          for _ in $(seq 1 90); do
            state="$(docker inspect -f '{{.State.Status}}' jlmirror-d3-keycloak 2>/dev/null || true)"
            case "$state" in
              exited|dead)
                echo "Keycloak exited before readiness: state=$state" >&2
                docker logs jlmirror-d3-keycloak >&2 || true
                exit 1
                ;;
              running)
                if python3 - <<'PY'
          import urllib.request
          try:
              with urllib.request.urlopen(
                  "https://127.0.0.1:8443/realms/master/.well-known/openid-configuration",
                  timeout=2,
              ) as response:
                  raise SystemExit(0 if response.status == 200 else 1)
          except Exception:
              raise SystemExit(1)
          PY
                then
                  echo 'keycloak_tls_readiness=PASS'
                  exit 0
                fi
                ;;
              '') ;;
              *) echo "Keycloak unexpected container state before readiness: $state" >&2 ;;
            esac
            sleep 1
          done
          echo 'Keycloak TLS readiness deadline exceeded' >&2
          docker inspect jlmirror-d3-keycloak >&2 || true
          docker logs jlmirror-d3-keycloak >&2 || true
          exit 1

      - name: Exercise real Authorization Code + PKCE/BFF contract
        shell: bash
        run: |
          set -euo pipefail
          python3 -m py_compile implementation/d3-identity-security/harness/keycloak_browser_probe.py
          python3 implementation/d3-identity-security/harness/keycloak_browser_probe.py

      - name: Exercise real back-channel logout token contract
        shell: bash
        run: |
          set -euo pipefail
          python3 -m py_compile implementation/d3-identity-security/harness/keycloak_backchannel_probe.py
          python3 implementation/d3-identity-security/harness/keycloak_backchannel_probe.py

      - name: Record pinned candidate evidence boundary
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          printf 'd3_keycloak_browser_candidate=PASS_PINNED_TLS\n'
          printf 'd3_keycloak_backchannel_candidate=PASS_PINNED_TLS\n'
          printf 'candidate_digest=%s\n' "$KEYCLOAK_IMAGE"
          printf 'probe_scope=historical_partial_only current_run_auto_credit=false\n'
          printf 'd3_a_current_state=accepted_candidate d3=separately_accepted\n'
          printf 'wave4=not_granted production=none d4=not_selected_not_granted\n'

      - name: Capture Keycloak logs on failure
        if: failure()
        shell: bash
        run: docker logs jlmirror-d3-keycloak 2>&1 || true

      - name: Retire Keycloak candidate container
        if: always()
        shell: bash
        run: docker rm -f jlmirror-d3-keycloak >/dev/null 2>&1 || true

      - name: Remove ephemeral TLS private material
        if: always()
        shell: bash
        run: rm -rf "${RUNNER_TEMP}/d3-keycloak-tls"
