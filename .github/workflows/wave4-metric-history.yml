name: Wave 4 Metric History

on:
  pull_request:
    paths:
      - 'src/jlmirror_monitoring/metric_history.py'
      - 'sql/wave4/040_zabbix_metric_history.sql'
      - 'sql/wave4/041_zabbix_metric_history_authority.sql'
      - 'tests/wave4/test_zabbix_metric_history.py'
      - 'tools/wave4/run_zabbix_metric_history_postgres_conformance.sh'
      - '.github/workflows/wave4-metric-history.yml'
  push:
    branches:
      - main
    paths:
      - 'src/jlmirror_monitoring/metric_history.py'
      - 'sql/wave4/040_zabbix_metric_history.sql'
      - 'sql/wave4/041_zabbix_metric_history_authority.sql'
      - 'tests/wave4/test_zabbix_metric_history.py'
      - 'tools/wave4/run_zabbix_metric_history_postgres_conformance.sh'
      - '.github/workflows/wave4-metric-history.yml'

permissions: {}

concurrency:
  group: wave4-metric-history-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  metric-history:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      POSTGRES_IMAGE: postgres@sha256:4ef4dbc939d61acea57712655ddb4b4ab27419c913f94cca0cd57cb3ea3c2280
      PG_CONTAINER: jlmirror-wave4-metric-history-postgres
      PG_PASSWORD: wave4-monitoring-password
      PG_DATABASE: jlmirror
    steps:
      - name: Resolve exact analyzed HEAD
        id: analyzed
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          EVENT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            resolved_sha="$PR_HEAD_SHA"
          else
            resolved_sha="$EVENT_SHA"
          fi
          test -n "$resolved_sha"
          printf 'sha=%s\n' "$resolved_sha" >> "$GITHUB_OUTPUT"

      - name: Checkout exact analyzed HEAD
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ steps.analyzed.outputs.sha }}
          fetch-depth: 0
          persist-credentials: false
          allow-unsafe-pr-checkout: false

      - name: Verify exact commit identity
        shell: bash
        env:
          EXPECTED_SHA: ${{ steps.analyzed.outputs.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"

      - name: Test bounded Metric History contracts
        run: PYTHONPATH=src python3 -m unittest tests.wave4.test_zabbix_metric_history

      - name: Execute Metric History composed PostgreSQL conformance
        run: bash tools/wave4/run_zabbix_metric_history_postgres_conformance.sh

      - name: Validate repository structure and workflow safety
        run: python3 tools/assurance/validate_repository.py
