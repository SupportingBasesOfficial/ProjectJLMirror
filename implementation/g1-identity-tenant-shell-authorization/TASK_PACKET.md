# G1 Identity + Tenant + Protected Shell — AI Task Packet

```text
SLICE: g1.identity-tenant-protected-shell@1
GOAL: Authenticated user enters a tenant-scoped JLMirror shell while current platform authorization is enforced and forbidden/cross-tenant context fails closed.
BASE SHA: 7c20c9301711e9a4bf355517d7aa23faad7a05b7
AUTHORITY: proposed g1.identity-tenant-protected-shell@1 authorization; accepted Wave 1 identity/BFF substrate; separately accepted D3 Identity/Security; canonical API/BFF/security contracts; AI E2E delivery constitution.
DEPENDENCIES: accepted Waves 1–3 substrate; G0 bootstrap only where directly required by this G1 slice.
IMPLEMENTATION BRANCH PREFIX: impl/g1-identity-tenant-protected-shell.
IMPLEMENTATION PR LABEL: jlmirror-slice:g1-identity-tenant-shell.
IMPLEMENTATION CLAIM: implementation/g1-identity-tenant-shell/IMPLEMENTATION_CLAIM.json with schema_version=1, authorization_id=g1.identity-tenant-protected-shell@1 and slice_id=g1.identity-tenant-protected-shell@1.
IMPLEMENTATION REPOSITORY: head and base MUST be the same canonical repository.
IMPLEMENTATION BASE: PR base ref MUST equal the repository current default branch.
ALLOWED PATH PREFIXES: apps/g1-identity-tenant-shell/; contracts/g1-identity-tenant-shell/; implementation/g1-identity-tenant-shell/; sql/g1/; src/jlmirror_g1/; tests/g1/; tools/g1/.
ALLOWED EXACT PATHS: .github/workflows/g1-identity-tenant-shell-runtime.yml.
TRUSTED SCOPE ATTESTATION: after the implementation PR exact HEAD is ready for scope proof, post /jlmirror-g1-scope-attest on that PR. The canonical .github/workflows/g1-identity-tenant-shell-implementation-scope.yml is an issue_comment workflow loaded from the default branch, not from the candidate. It resolves PR metadata through the GitHub API, requires base ref == current default branch, fetches base/head commits only as Git objects, materializes tools/assurance/validate_g1_identity_tenant_shell_implementation_scope.py from the exact default-branch base commit with git show, and validates the complete base...head diff with --no-renames.
TRUSTED STATUS: canonical context is JLMIRROR / g1-identity-tenant-shell-implementation-scope. The trusted caller publishes pending to the exact resolved PR HEAD before analysis and publishes final success only if analysis passes and current HEAD/base SHA/base ref still equal the resolved values. Any drift publishes failure/stale evidence on the resolved HEAD and requires a rerun.
BASE-ADVANCE INVALIDATION: a trusted push to the repository current default branch scans open PRs targeting that branch. Any head whose latest canonical G1 scope context is success is immediately replaced with pending/stale evidence, so a prior attestation cannot remain green after the default-branch advance. The PR must rerun /jlmirror-g1-scope-attest on the new exact base.
FRESHNESS/CONCURRENCY: attestation and default-branch invalidation runs use cancel-in-progress=true so superseded evidence cannot overwrite newer evidence. Status-write jobs are isolated from code analysis and do not checkout or execute candidate Python.
CLASSIFICATION: invocation of the trusted default-branch attestation is the independent G1 implementation classification. The validator has no candidate-controlled relevance/not-g1 success path. Missing branch prefix, label, claim, same-repository identity, changed paths or any path outside the allowlist fails closed. A shared-core-only diff with all voluntary G1 metadata omitted MUST fail once attested.
EXACT-HEAD RULE: the trusted scope attestation is valid only for the exact HEAD SHA and base coordinates resolved by that run. If HEAD, base SHA or base ref changes, or the default branch advances afterward, rerun /jlmirror-g1-scope-attest. Candidate-controlled pull_request orchestration is not authoritative for scope enforcement. pull_request_target remains forbidden.
SHARED EXISTING PATHS: read-only unless a separate successor authorization explicitly grants a specific change. The implementation PR MUST validate its complete diff against this exact prefix/exact-path policy; it may not self-declare additional paths as G1-specific.
FORBIDDEN: G2+ product behavior; Monitoring UI; Resource/Metric/Problem/Health UI; Alerting policy/lifecycle; ACK/notification; ITSM; Automation; AIOps; FinOps; Commercial; production deployment; speculative generic design/navigation systems.
DB: Reuse accepted durable Identity/session/current-authority ownership. Add only persistence/bootstrap required by G1 and only when existing accepted substrate does not already provide it.
DOMAIN: No new identity or authorization semantics. JWT validity never equals current authorization. IdP-native roles/groups/orgs never become platform membership/permission truth.
API/BFF: Confidential BFF. OIDC Authorization Code + PKCE S256. Browser receives only opaque server-side session capability. Current tenant/membership/permission/placement/auth-strength authority precedes protected reads/effects. Client tenant identifiers are never authority.
FRONTEND: Minimal protected application shell with tenant context and loading/success/unauthenticated/forbidden/unavailable/revoked-or-stale-authority states. No backend-shape-driven navigation expansion.
FAILURE: IdP new-login/step-up uncertainty fails closed; current-authority uncertainty fails closed; revoked/forbidden tenant is denied without existence leakage; stale positive cache/session state cannot override current durable authority.
SECURITY: Tenant isolation; CSRF explicit; no refresh token or long-lived platform credential in browser JS; forced logout/revocation cannot be masked by stale cache; provider/workload/network identity is never business authority.
TESTS: Unit + integration + real persistence + browser E2E + cross-tenant + revoked permission + suspended/forbidden tenant + stale-session/current-authority adversarial cases.
RUN: Clean checkout -> create canonical G1 implementation branch -> target repository default branch -> attach canonical G1 PR label -> create implementation claim -> start G1 dependencies -> migrate/bootstrap -> seed test identity/tenant -> start BFF/backend/frontend -> execute allowed browser journey -> execute forbidden/cross-tenant journey -> container proof -> exact-head CI -> post /jlmirror-g1-scope-attest -> verify status JLMIRROR / g1-identity-tenant-shell-implementation-scope is success on the exact current implementation HEAD and resolved base coordinates are still current.
DONE: E2E-16 evidence for this slice, exact-head CI, trusted scope attestation success on the same exact HEAD/base, and browser proof. Human patching of DB/source during the journey is forbidden.
STOP IF: Trusted scope attestation cannot be produced on the exact implementation HEAD/base; PR does not target the current default branch; default-branch advance has invalidated the prior success and a fresh attestation has not passed; required canonical branch/label/claim metadata cannot be satisfied; any required product semantic is not already authorized; implementation needs any path outside the exact policy above; implementation must modify shared existing substrate; implementation needs G2+ behavior; tenant/current-authority owner is ambiguous; a framework/provider feature would become canonical business authority; production-only C3 numeric/topology choice becomes necessary.
```

This packet is a bounded projection of repository truth. It is not merge authority and is not permission to implement before the accompanying authorization becomes canonical.
